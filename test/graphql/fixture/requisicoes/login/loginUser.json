{
    "query": "mutation LoginUser($username: String!, $password: String!) { loginUser(username: $username, password: $password) { token }}",
    "variables": {
        "username": "warlley",
        "password": "123456"
    }
}